import { ColorGeneroDirective } from './color-genero.directive';

describe('ColorGeneroDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorGeneroDirective();
    expect(directive).toBeTruthy();
  });
});
